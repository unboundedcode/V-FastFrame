<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme.activityAnimation">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>

    <!--  values-v19。v19 开始有 android:windowTranslucentStatus 这个属性 -->
    <style name="TranslucentTheme" parent="BaseTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="SwipeBackTheme" parent="TranslucentTheme"/>

</resources>
